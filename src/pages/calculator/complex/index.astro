---
import BasicLayout from "../../../layouts/BasicLayout.astro"
import { getCollection } from 'astro:content';

const docs = await getCollection('learn');
---

<BasicLayout title="Complex | demomath.org", genSidebar={false}>
    <h1>Welcome to the complex calculator Page!</h1>
    <p>This content is wrapped neatly inside the global layout structure.</p>
</BasicLayout>